<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greenhi.peachland.mapper.CommentMapper">

    <resultMap id="commentMap" type="com.greenhi.peachland.item.ItemCommentBase">
        <id column="id" property="id"/>
        <result column="uid" property="uid"/>
        <result column="user_name" property="userName"/>
        <result column="dynamic_id" property="dynamicId"/>
        <result column="comment_content" property="commentContent"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByDid" resultMap="commentMap">
        select c.id,c.uid,u.user_name,dynamic_id,comment_content,c.create_time
        from comment c
        inner join user u
        on c.uid=u.id
        where dynamic_id=#{did}
    </select>

</mapper>
